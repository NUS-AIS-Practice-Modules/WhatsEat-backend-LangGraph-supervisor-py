# Copy to .env and fill in the credentials required by the LangGraph backend.
# Used by whats_eat/app/supervisor_app.py via langchain's init_chat_model.
OPENAI_API_KEY=sk-...

# Required for Google Places and Maps tooling.
GOOGLE_MAPS_API_KEY=your_google_maps_key
# Optional browser key if you split server/browser credentials.
# GOOGLE_MAPS_BROWSER_KEY=your_browser_key

# Optional LangSmith tracing (disabled by default).
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key
# LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
